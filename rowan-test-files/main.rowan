import std::collections::ArrayList;

class Main {

    static thing: u64 = 66666666666;

    fn main(args: [String]) {
        let printer: Printer = new Printer();
        let n: u64 = 40
        let result: u64 = Main::fib(n);
        printer.println-int(result);
    }

    fn fib(n: u64) -> u64 {
        if n < 2 {
            return n;
        }
        let first: u64 = n - 1;
        let second: u64 = n - 2;
        let left: u64 = Main::fib(first);
        let right: u64 = Main::fib(second);
        return left + right;
    }

    fn from-array(arr: [u64]) {
        let x: u64 = 0;
    }

}
