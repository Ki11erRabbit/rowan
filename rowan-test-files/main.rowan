module Main;

class Main {
    @Override
    fn ready() {
        let x: u64 = 4 + 4;
        let printer: Printer = new Printer();
        printer.println-int(x);
        if False {
            printer.println-int(x);
        } else {
            printer.println-int(x + x);
        }
    }
}
