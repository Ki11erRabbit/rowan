module Main;

class Main {
    @Override
    fn ready() {
        let x: u64 = 4 + 4;
        let printer: Printer = new Printer();
        printer.println-int(x);
        while x < 50 {
            printer.println-int(x);
            x = x + x;
        }
    }
}
