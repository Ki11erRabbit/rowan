module Main;

class Main {
    @Override
    fn ready(self) {
        let x: u64 = 4 + 4;
        let printer: Printer = new Printer();
        printer.println-int(x);
        if x < 10 {
            printer.println-int(x);
        }
        while x < 50 {
            printer.println-int(x);
            x = x + x;
        }
        printer.println-int(x);
        let arr: [u64] = [1, 2 ,3, 4];
        let counter: u64 = 0;
        self.do-thing();
        while counter < arr.len() {
            printer.println-int(arr[counter]);
            counter = counter + 1;
        }
    }

    fn do-thing(self) {
        let printer: Printer = new Printer();
        let arr: [u64] = [1, 2 ,3, 4];
        arr[5];
        printer.println-int(arr[5]);

        let y: u64 = 11;
        printer.println-int(y);
    }
}
